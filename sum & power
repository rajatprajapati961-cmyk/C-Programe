# include <stdio.h>
# include <math.h>
int main() {
    int sum, b, c, power;
    b = c = 1;
    sum = b + c;
    power = sum * sum;
    scanf("%d", &sum);
    printf("The value of a is: %d\n", sum);
    printf("The value of power is: %d\n", power);
    return 0;
}

